name: Android CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  Build:
    name: Build/Sign APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      #      - name: Build APK
      #        id: build
      #        run: ./gradlew assembleRelease

      - name: Decode Keystore
        env:
          ENCODED_STRING: ${{ secrets.GIT_JKS_FILE }}
        run: |
          TMP_KEYSTORE_FILE_PATH="${RUNNER_TEMP}"/keystore
          mkdir "${TMP_KEYSTORE_FILE_PATH}"
          echo $ENCODED_STRING | base64 -d > "${TMP_KEYSTORE_FILE_PATH}"/keystore.jks


      - name: Build APK
        run: ./gradlew assembleRelease -Pandroid.injected.signing.store.file= "${TMP_KEYSTORE_FILE_PATH}"/keystore.jks -Pandroid.injected.signing.store.password=${{ secrets.GIT_STORE_PASSWORD }} -Pandroid.injected.signing.key.alias=${{ secrets.GIT_KEY_ALIAS }} -Pandroid.injected.signing.key.password=${{ secrets.GIT_KEY_PASSWORD }}

      - name: Remove keystore file
        run: rm "${TMP_KEYSTORE_FILE_PATH}"/keystore.jks


      #      - name: Sign APK
      #        id: sign_apk
      #        uses: r0adkll/sign-android-release@v1
      #        with:
      #          releaseDirectory: app/build/outputs/apk/release
      #          signingKeyBase64: ${{ secrets.GIT_JKS_FILE }}
      #          alias: ${{ secrets.GIT_KEY_ALIAS }}
      #          keyStorePassword: ${{ secrets.GIT_STORE_PASSWORD }}
      #          keyPassword: ${{ secrets.GIT_KEY_PASSWORD }}

      - name: Make artifact
        uses: actions/upload-artifact@v2
        with:
          name: app-release-signed
          path: app/build/outputs/apk/release

  Release:
    name: Create Release/Upload APK
    needs: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: app-release-signed
          path: app/build/outputs/apk/release

      - name: Build Changelog
        id: changelog
        uses: ardalanamini/auto-changelog@v3
        with:
          mention-authors: false
          mention-new-contributors: false
          include-compare: false
          semver: false

      - name: Expose version name
        uses: michpohl/android-expose-version-name-action@v1.0.0
        with:
          expose-version-name: 'true'
          expose-version-code: 'true'

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        with:
          tag_name: ${{ env.ANDROID_VERSION_NAME }}
          release_name: Release ${{ env.ANDROID_VERSION_NAME }}
          body: ${{ steps.changelog.outputs.changelog }} &#x20;
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: List APKs
        run: ls -R app/build/outputs/apk/release/

      - name: Upload APK
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: app/build/outputs/apk/release/app-release.apk
          asset_name: app-release-signed-${{ env.ANDROID_VERSION_NAME }}.apk
          asset_content_type: application/zip
